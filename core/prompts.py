SYSTEM_PROMPT = """
Ты — экспертный AI-агент для управления браузером. Твоя цель — автономно выполнить задачу пользователя, управляя интерфейсом как человек.
Язык общения и мыслей: Русский.

### 🧠 МЕТОДОЛОГИЯ РАБОТЫ (CHAIN OF THOUGHT)
Перед каждым вызовом инструмента ты ОБЯЗАН сформулировать план в блоке мыслей. Формат:
1. **Наблюдение:** Что я вижу на текущей странице? (анализ title, URL, ключевых элементов).
2. **Анализ:** Как это соотносится с целью? (я на верном пути или в тупике?).
3. **План:** Какой следующий шаг самый логичный? (клик, поиск, скролл).
4. **Действие:** Вызов конкретного инструмента.

### 🛠 ПРИНЦИПЫ ИСПОЛЬЗОВАНИЯ ИНСТРУМЕНТОВ
1. **Золотое правило:** НИКОГДА не действуй вслепую.
   - В начале работы и после каждой навигации вызывай `analyze_page()`.
   - Используй ТОЛЬКО те `element_id`, которые вернул последний `analyze_page()`. Не выдумывай ID.
    - Базовый путь: `analyze_page()` → `click_element`/`type_text`/`click_and_type`.
    - Если `wait_for_element` показывает текст нужной кнопки, но она по какой-то причине не попала в последнюю дистилляцию, в крайнем случае можно использовать `click_text()` по её видимому тексту.
2. **Работа с контентом:**
   - Для поиска (товары, статьи) используй `click_and_type(id, text)`. Это надежнее, чем клик + ввод.
   - Если страница пустая или элемент не виден -> `scroll_page()`.
   - Для списков (письма, вакансии) используй `collect_elements()`, чтобы не тратить токены на лишние клики.
3. **Обработка ошибок (SELF-CORRECTION):**
   - Если инструмент вернул ошибку (напр. "Element not found"):
     а) Не паникуй. Страница могла обновиться.
     б) Вызови `analyze_page()`, чтобы получить актуальные ID.
     в) Повтори действие с новым ID.
   - Если попал в тупик -> `go_back()` или начни поиск заново.
4. **Проверка результата через `query_dom`:**
   - После важных действий (добавление в корзину, удаление писем, отклик на вакансии, смена адреса доставки) проверяй эффект с помощью `query_dom`.
   - Формулируй конкретные вопросы, например: "Товар успешно добавлен в корзину? Что показывает кнопка корзины в шапке (количество и сумма)?"
   - Используй ответ, чтобы решить: действие прошло, нужно повторить, откатиться (`go_back`) или сообщить о проблеме сайта.
5. **Если click_element даёт timeout, но текст нужной кнопки виден:**
   - Сделай повторный `analyze_page()` и попробуй `click_text()` по тому же видимому тексту этой кнопки,
     прежде чем сдаваться.

### 🛡 БЕЗОПАСНОСТЬ И ОГРАНИЧЕНИЯ
- **Подтверждение:** Перед оплатой, удалением данных или отправкой сообщений — спроси подтверждение у пользователя.
- **Конфиденциальность:** Не читай и не запоминай личные данные, если это не нужно для задачи.
- **Запреты:** Не запрашивай исходный HTML страницы (это слишком много токенов). Работай через дистилляцию (`analyze_page`).

### 💡 СПЕЦИАЛЬНЫЕ СЦЕНАРИИ
- **Поиск товаров/услуг:**
  - Если видишь карточку товара/ресторана — можешь кликнуть по ней, чтобы провалиться внутрь, если это помогает лучше понять товар и выполнить задачу пользователя.
  - Не делай вывод, что товар уже добавлен в корзину только по тому, что в модальном окне есть счётчик количества (например, кнопки изменения количества и число «1»). Считай такие элементы настройкой количества, а не подтверждением успешного добавления.
  - Факт добавления в корзину проверяй по изменениям интерфейса (значок корзины, количество/сумма, явные сообщения на странице) с помощью `query_dom`, а не по одному отдельному индикатору.
  - Если после проверки видно, что действие не сработало, не зацикливайся на повторном открытии одной и той же карточки: попробуй другие интерактивные элементы, которые по смыслу могут привести к нужному результату (кнопки рядом с карточкой, элементы в шапке и т.п.).
  - Если после нескольких разумных попыток ничего не работает — явно опиши пользователю, какие действия ты пробовал, и что сайт, похоже, ведёт себя некорректно.


Твоя цель — успех задачи. Если задача выполнена, выведи финальный результат и остановись.
"""


TOOLS = [
    {
        "name": "analyze_page",
        "description": """
Собирает дистилляцию интерактивных элементов страницы с семантическими ID.

When to use:
- Перед любыми кликами или вводом; сразу после навигации.
- После ошибок, чтобы обновить список элементов.

Examples:
- analyze_page()
- analyze_page(response_format="detailed")

Important:
- Всегда вызывай перед click_element/type_text/click_and_type.
- Используй только ID из последнего analyze_page().

Common errors:
- "Failed to analyze page" → Try: 1) wait_for_element(), 2) close_modal(), 3) повтори analyze_page().
- "No elements returned" → scroll_page(), затем analyze_page().
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "response_format": {
                    "type": "string",
                    "enum": ["concise", "detailed"],
                    "default": "concise",
                }
            },
        },
    },
    {
        "name": "click_element",
        "description": """
Кликает по элементу по его ID из analyze_page().

When to use:
- Для кнопок, ссылок, переключателей.
- Перед вводом, если не используешь click_and_type.

Examples:
- click_element(element_id=5)
- click_element(element_id=17)

Important:
- ID только из последнего analyze_page().
- Если открылась новая вкладка, используй switch_to_page().

Common errors:
- "Unknown element ID" → analyze_page() и возьми свежий ID.
- "Timeout/Not clickable" → Try: 1) scroll_page(), 2) close_modal(), 3) analyze_page() и повтори.
""",
        "input_schema": {
            "type": "object",
            "properties": {"element_id": {"type": "integer"}},
            "required": ["element_id"],
        },
    },
    {
        "name": "type_text",
        "description": """
Вводит текст в поле по ID из analyze_page(), опционально нажимает Enter.

When to use:
- Ввод в инпут/textarea, если клик сделан отдельно.
- Формы/поиск без атомарного шага.

Examples:
- type_text(element_id=2, text="Makfa 2 кг", press_enter=True)
- type_text(element_id=10, text="test@example.com")

Important:
- Предпочитай click_and_type для поиска.
- ID только из последнего analyze_page().

Common errors:
- "Unknown element ID" → analyze_page() для новых ID.
- "Element not typeable" → Try: 1) click_element() по полю, 2) close_modal(), 3) повтори type_text().
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "element_id": {"type": "integer"},
                "text": {"type": "string"},
                "press_enter": {"type": "boolean", "default": False},
            },
            "required": ["element_id", "text"],
        },
    },
    {
        "name": "click_and_type",
        "description": """
Кликает по полю и сразу вводит текст (по умолчанию с Enter).

When to use:
- Поиск товаров/писем/вакансий.
- Поля, где клик+ввод нужен одним шагом.

Examples:
- click_and_type(element_id=2, text="мука Makfa 2кг")
- click_and_type(element_id=5, text="QA engineer", press_enter=True)

Important:
- ID из последнего analyze_page().
- Если не сработало — scroll_page() → analyze_page() → повтори.

Common errors:
- "Unknown element ID" → обнови через analyze_page().
- "Timeout/Not typeable" → Try: 1) click_element(), 2) close_modal(), 3) повтори click_and_type().
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "element_id": {"type": "integer"},
                "text": {"type": "string"},
                "press_enter": {"type": "boolean", "default": True},
            },
            "required": ["element_id", "text"],
        },
    },
    {
        "name": "navigate_url",
        "description": """
Открывает заданный URL в браузере.

When to use:
- Старт сценария или переход на нужный сайт.
- Перезагрузка при ошибках.

Examples:
- navigate_url(url="https://example.com")
- navigate_url(url="https://example.com/login")

Important:
- После навигации сразу analyze_page().
- Добавь https:// если нет схемы.

Common errors:
- "NavigationError" → проверь URL, повтори navigate_url(), затем analyze_page().
- "Timeout" → дождись через wait_for_element() и повтори.
""",
        "input_schema": {
            "type": "object",
            "properties": {"url": {"type": "string"}},
            "required": ["url"],
        },
    },
    {
        "name": "take_screenshot",
        "description": """
Делает скриншот страницы, опционально возвращает base64.

When to use:
- Диагностика, когда элементы не видны.
- Перед сообщением об ошибке/тупике.

Examples:
- take_screenshot(label="before_checkout")
- take_screenshot(label="error_state", embed_b64=True)

Important:
- base64 обрезается, чтобы не раздувать контекст.
- Храни path для ручной проверки.

Common errors:
- "ScreenshotError" → убедись, что страница загружена, повтори позже.
- "Directory not configured" → укажи --screenshot-dir или embed_b64=True.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "label": {"type": "string"},
                "embed_b64": {"type": "boolean", "default": False},
            },
        },
    },
    {
        "name": "wait_for_element",
        "description": """
Ждёт появления текста на странице.

When to use:
- После навигации/клика, если контент грузится.
- Перед повторной попыткой действия.

Examples:
- wait_for_element(query="Нужный текст", timeout=5)

Important:
- После ожидания — analyze_page() для актуальных ID.
- Не ставь слишком большие таймауты без причины.

Common errors:
- "Timeout" → увеличь timeout, take_screenshot(), analyze_page().
- "Element not found" → уточни текст, прокрути и повтори.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string"},
                "timeout": {"type": "number", "default": 5},
            },
            "required": ["query"],
        },
    },
    {
        "name": "scroll_page",
        "description": """
Прокручивает страницу вверх/вниз на заданное число пикселей.

When to use:
- Элементы вне видимой области.
- Подгрузка списка перед analyze_page()/collect_elements().

Examples:
- scroll_page(direction="down", amount=800)
- scroll_page(direction="up", amount=400)

Important:
- После скролла обнови контекст через analyze_page().
- Скроль по чуть-чуть, чтобы не пропустить нужное.

Common errors:
- "ScrollError" → уменьшить amount, попробовать click_element() по странице, затем снова scroll_page().
- "Page not initialized" → убедись, что сессия открыта.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "direction": {"type": "string", "enum": ["up", "down"], "default": "down"},
                "amount": {"type": "integer", "default": 800},
            },
        },
    },
    {
        "name": "switch_to_page",
        "description": """
Фокусируется на вкладке/странице по индексу.

When to use:
- Клик открыл новый таб, нужно перейти.
- Вернуться на первую вкладку.

Examples:
- switch_to_page(index=-1)
- switch_to_page(index=0)

Important:
- После переключения — analyze_page() для новых ID.
- Индексация Playwright: 0 — первая вкладка.

Common errors:
- "No open pages" → вкладок нет, оставайся на текущей.
- "Page index out of range" → проверь количество вкладок.
""",
        "input_schema": {
            "type": "object",
            "properties": {"index": {"type": "integer", "default": -1}},
        },
    },
    {
        "name": "go_back",
        "description": """
Возвращается назад в истории.

When to use:
- Клик привёл в тупик/не туда.
- Нужно откатить последний переход.

Examples:
- go_back()
- go_back()  # после неудачного раздела

Important:
- После возврата — analyze_page().
- Если истории нет, переходи navigate_url().

Common errors:
- "NavigationError" → повтори go_back() или navigate_url().
- "No history to go back" → иди напрямую через navigate_url().
""",
        "input_schema": {"type": "object", "properties": {}},
    },
    {
"name": "click_text",
"description": """
Кликает по элементу по видимому тексту (Playwright get_by_text).
Основной путь работы — через analyze_page() + click_element(), но
если нужная кнопка не попала в дистилляцию и её видно на экране,
можно использовать click_text по её видимому тексту как запасной вариант.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "text": {"type": "string"},
                "exact": {"type": "boolean", "default": False},
            },
            "required": ["text"],
        },
    },
    {
        "name": "search_elements",
        "description": """
Ищет в последней дистилляции по тексту/aria/placeholder/href.

When to use:
- Быстрый поиск ID по ключевым словам.
- Когда не хочешь повторять analyze_page().

Examples:
- search_elements(query="текст на кнопке", max_results=5)

Important:
- Работает на последнем analyze_page(); при сомнении — обнови.
- Далее действуй через click_element/type_text по найденным ID.

Common errors:
- "No recent elements" → вызови analyze_page().
- "Empty results" → другой запрос или detailed analyze_page().
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string"},
                "max_results": {"type": "integer", "default": 5},
            },
            "required": ["query"],
        },
    },
    {
        "name": "extract_text",
        "description": """
Извлекает видимый текст по CSS-селектору.

When to use:
- Прочитать письмо/описание товара/вакансии без кликов.
- Когда дистилляции недостаточно.

Examples:
- extract_text(selector=".product-card", all_matches=True)
- extract_text(selector="#body", max_chars=2000)

Important:
- Селектор должен существовать; при ошибке уточни.
- Текст обрезается max_chars.

Common errors:
- "Element not found" → проверь селектор, сделай analyze_page().
- "Timeout" → увеличь ожидание, прокрути и повтори.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "selector": {"type": "string"},
                "all_matches": {"type": "boolean", "default": False},
                "max_chars": {"type": "integer", "default": 4000},
            },
            "required": ["selector"],
        },
    },
    {
        "name": "collect_elements",
        "description": """
Собирает список элементов (text/href/aria/title) по селектору для длинных списков.

When to use:
- Списки писем, товаров, вакансий без кликов.
- Чтобы выбрать нужное по тексту.

Examples:
- collect_elements(selector=".product-card", limit=10)
- collect_elements(selector="[data-testid*='item']", limit=5)

Important:
- Подбирай корректный селектор карточек.
- Ограничивай limit, чтобы не раздувать контекст.

Common errors:
- "No items" → scroll_page(), другой селектор, analyze_page() для ориентира.
- "CollectError" → убедись, что страница загружена, повтори позже.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "selector": {"type": "string"},
                "limit": {"type": "integer", "default": 10},
            },
            "required": ["selector"],
        },
    },
    {
        "name": "switch_frame",
        "description": """
Переключает контекст в iframe по селектору или индексу (index=-1 — в основной).

When to use:
- Целевой элемент внутри iframe.
- Нужно вернуться в основной фрейм.

Examples:
- switch_frame(selector="iframe[data-testid='app']")
- switch_frame(index=-1)

Important:
- После переключения — analyze_page() в новом контексте.
- selector приоритетнее index.

Common errors:
- "Frame not found" → проверь селектор, сделай analyze_page() для iframe.
- "Frame index out of range" → уточни число фреймов; index=-1 вернёт в основной.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "selector": {"type": "string"},
                "index": {"type": "integer", "default": 0},
            },
        },
    },
    {
        "name": "close_modal",
        "description": """
Закрывает модалки/оверлеи (кнопки закрытия или Escape).

When to use:
- Клики блокирует баннер/попап.
- Перед повторной попыткой взаимодействия.

Examples:
- close_modal()
- close_modal()  # если перекрытие

Important:
- После закрытия при необходимости analyze_page().
- Может нажать Escape, если кнопок нет.

Common errors:
- "Failed to close modal" → scroll_page(), analyze_page() и кликни крест вручную, повтори close_modal().
- "No modal found" → если оверлея нет, продолжай сценарий.
""",
        "input_schema": {"type": "object", "properties": {}},
    },
    {
        "name": "validate_task_complete",
        "description": """
Возвращает title/URL/текст страницы для проверки успеха.

When to use:
- В конце сценария или для промежуточной проверки (корзина, подтверждение).
- Когда нужно логически проверить результат.

Examples:
- validate_task_complete()
- validate_task_complete(hint="Проверь, что товар в корзине")

Important:
- Анализируй text_sample, не только URL.
- Используй вместе с take_screenshot() при сомнениях.

Common errors:
- "ValidationError" → сделай take_screenshot(), analyze_page(), оцени вручную.
- "Empty text_sample" → убедись, что страница загружена, повтори вызов.
""",
        "input_schema": {
            "type": "object",
            "properties": {"hint": {"type": "string"}},
        },
    },
    {
        "name": "query_dom",
        "description": """
Отвечает на уточняющие вопросы о текущей странице на основе дистиллированного DOM.

When to use:
- После важных действий: добавление товара в корзину, удаление писем, отклик на вакансии.
- Для проверки, что действие сработало (счётчик, появление элементов).

Examples:
- query_dom(query="Товар успешно добавлен в корзину? Что показывает кнопка корзины в шапке?")
- query_dom(query="Сколько подходящих вакансий видно в списке и какие у них названия?")

Important:
- Этот инструмент ничего не кликает и не вводит, он только описывает состояние страницы.
- Работает поверх результата analyze_page(response_format="detailed").

Common errors:
- "Не удалось получить снимок" → смотри analyze_page(), сделай scroll_page()/wait_for_element()/close_modal() и повтори.
- "DOM-подагент не инициализирован" → проверь создание DomSubAgent и присвоение executor.dom_agent.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "query": {"type": "string"},
            },
            "required": ["query"],
        },
    },
    {
        "name": "finish_task",
        "description": """
Завершает работу агента и возвращает итоговый отчёт.

When to use:
- Когда задача пользователя полностью выполнена.
- Когда действие выполнено, и нужно остановиться, чтобы не продолжать лишние шаги.

Examples:
- finish_task(summary="Товар добавлен в корзину, оплата не производилась.")
- finish_task(summary="Удалил спам-письма, важные письма оставил.")

Important:
- После успешного добавления в корзину (если пользователь просил не оплачивать) вызывай finish_task вместо продолжения.
- Не выполняй больше инструментов после finish_task.

Common errors:
- "summary is empty" → передай краткое описание выполненных шагов.
- "Called too early" → убедись, что нужные проверки (query_dom/validate_task_complete) уже показали успех.
""",
        "input_schema": {
            "type": "object",
            "properties": {
                "summary": {"type": "string"},
            },
            "required": ["summary"],
        },
    },
]
